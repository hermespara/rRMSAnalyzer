% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_3d_structure.R
\name{plot_3d_structure}
\alias{plot_3d_structure}
\title{Plot 3D rRNA structure with annotated sites}
\usage{
plot_3d_structure(
  ribo,
  pdb_id = "4UG0",
  chain_mapping = NULL,
  site_color = "red",
  site_style = "sphere",
  label_sites = FALSE,
  cartoon_quality = 3,
  show_proteins = FALSE,
  background_color = "white",
  rna_color = "#D1D5DB",
  rna_opacity = 0.55,
  sphere_radius = 1.1,
  use_mmcif = TRUE,
  viewer_width = 900,
  viewer_height = 700,
  ...
)
}
\arguments{
\item{ribo}{A SummarizedExperiment object.}

\item{pdb_id}{A character string specifying the PDB ID (default: "4UG0").}

\item{chain_mapping}{A named list or vector mapping RNA names to PDB chain IDs.
Example: `c("28S" = "A5", "18S" = "AA")`. If NULL, uses defaults for 4UG0.}

\item{site_color}{Color for the highlighted sites (default: "red").}

\item{site_style}{Style for the highlighted sites: "sphere", "stick", or "cartoon" (default: "sphere").}

\item{label_sites}{Logical, whether to add labels to highlighted sites (default: TRUE).}

\item{cartoon_quality}{Quality of the cartoon representation (1-10, default: 3).
Lower values improve performance. Use 1-3 for ribosome.}

\item{show_proteins}{Logical, whether to show ribosomal proteins (default: FALSE).
Setting to FALSE dramatically improves performance for ribosome visualization.}

\item{background_color}{Background color (default: "white").}

\item{rna_color}{Color scheme for rRNA: "spectrum", "chain", "secondary", or specific color (default: "spectrum").}

\item{rna_opacity}{Opacity of RNA cartoon (0-1, default: 0.7).}

\item{sphere_radius}{Radius of spheres for highlighted sites (default: 2.5).}

\item{use_mmcif}{Logical, use mmCIF format instead of PDB (default: TRUE).
mmCIF handles large structures better.}

\item{viewer_width}{Width of the viewer in pixels (default: 800).}

\item{viewer_height}{Height of the viewer in pixels (default: 600).}

\item{...}{Additional arguments passed to `r3dmol::r3dmol()`.}
}
\value{
An `r3dmol` htmlwidget object.
}
\description{
This function visualizes the 3D structure of rRNA or the ribosome using `r3dmol`.
It highlights specifically annotated sites of interest. Optimized for large structures
like the human ribosome.
}
\details{
The function uses `r3dmol` to fetch a PDB structure and highlight residues corresponding
to the annotated sites in the `SummarizedExperiment` object. Mapping between
RNA names in the object and chains in the PDB structure is handled via `chain_mapping`.

Default mappings for human 80S ribosome (PDB 4UG0):
- '28S' mapped to chain 'A5' (LSU rRNA)
- '5S' mapped to chain 'A8' (LSU 5S)
- '5.8S' mapped to chain 'A6' (LSU 5.8S)
- '18S' mapped to chain 'AA' (SSU rRNA)

Note: Chain IDs may vary depending on PDB structure. Use `view_pdb_chains()` to inspect.
}
\examples{
\dontrun{
data("ribo_toy")
# View with default settings (rRNA cartoon, sites as spheres)
plot_3d_structure(ribo_toy)

# Different color scheme
plot_3d_structure(ribo_toy, rna_color = "lightblue")

# Show proteins (slower)
plot_3d_structure(ribo_toy, show_proteins = TRUE, cartoon_quality = 2)
}
}
